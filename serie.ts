export class Serie{
    constructor(public num:number, public name:string, public channel:string, public season:number, public description:string, public url:string, public image:string)
    {
        
    }
}